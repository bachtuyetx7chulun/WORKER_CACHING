const RANKING_WORKER = 'Ranking worker local'
const RSS_WORKER = 'Rss worker local'
const READING_PAGE_WORKER = 'ReadingPage worker local'
const CACHING_WORKER = 'Caching worker local'
const MOVILE_CACHING_WORKER = 'Mobile Caching worker local'
const TRIGGER_WORKER = 'Trigger batch update episode'
const PIANO_CACHING_WORKER = 'Piano caching worker'
const SIMPLE_CACHING_WORKER = 'Simple caching worker'
const DEFAULT_LOOP_TIME = 300000

export {
  RANKING_WORKER,
  RSS_WORKER,
  READING_PAGE_WORKER,
  CACHING_WORKER,
  TRIGGER_WORKER,
  MOVILE_CACHING_WORKER,
  PIANO_CACHING_WORKER,
  SIMPLE_CACHING_WORKER,
  DEFAULT_LOOP_TIME,
}
